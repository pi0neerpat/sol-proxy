{
  "name": "sol-proxy",
  "description": "A tool for making any Solidity contract into a proxy contract, per EIP 1822.",
  "version": "0.1.0",
  "main": "lib/index.js",
  "author": "pi0neerpat",
  "repository": "pi0neerpat/sol-proxy",
  "license": "GPL-3.0",
  "bin": "./bin/sol-proxy",
  "scripts": {
    "start": "npm run compile && npm run test",
    "compile": "babel src --out-dir lib",
    "test": "sol-proxy create contracts/RToken.sol",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "prepare": "npm run compile"
  },
  "devDependencies": {
    "babel-cli": "*",
    "babel-preset-env": "^1.6.1",
    "chai": "*",
    "concurrently": "^4.1.2",
    "coveralls": "*",
    "isparta": "*",
    "mocha": "^5.0.4",
    "sinon": "*"
  },
  "dependencies": {
    "c3-linearization": "^0.2.0",
    "colors": "^1.2.1",
    "graphviz": "0.0.8",
    "npm": "^5.7.1",
    "solidity-parser-antlr": "^0.2.8",
    "solpp": "^0.10.2",
    "yargs": "^11.0.0"
  },
  "nodemonConfig": {
    "ignore": [
      "lib/*"
    ]
  }
}
